<h1>New job opening</h1>

<div class="card">
  <%= form_with model: @job_opening, url: job_openings_path, local: true do |f| %>
    <%= render "form", f: f %>
    <%= f.submit "Create", class: "btn btn-primary" %>
  <% end %>
</div>
